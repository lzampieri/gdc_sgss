<!DOCTYPE html>
<html>
    <head>
        <!-- Fogli di stile -->
        <link rel="stylesheet" href="{{ url('/css/app.css' ) }}" />

        <!-- Caricamento (posticipato) di React -->
        <script src="{{ url('/js/app.js' ) }}" defer></script>
    </head>
    <body>
        <div id="root" />
    </body>
</html>
